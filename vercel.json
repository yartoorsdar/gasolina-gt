{
  "version": 2,
  "builds": [
    { "src": "web/**", "use": "@vercel/static" }
  ],
  "routes": [
    { "src": "/data/export/(.*)", "dest": "/data/export/$1" },
    { "src": "/(.*)", "dest": "/$1" }
  ]
}
